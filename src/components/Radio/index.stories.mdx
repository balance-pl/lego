import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import Radio from './index'
import * as stories from './index.stories.tsx'
import { DisableWithState, WithState } from './index.stories'

<Meta
  title="Компоненты/Radio"
  component={Radio}
  parameters={{
    actions: { argTypesRegex: '^on.*' },
  }}
  argTypes={{
    disabled: {
      control: 'boolean',
      description: '"disabled" состояние',
      defaultValue: false,
    },
    checked: {
      control: 'boolean',
      description: '"checked" состояние',
      defaultValue: false,
    },
    children: {
      control: { type: 'text' },
      description: 'Содержимое radio (текст)',
      table: {
        disable: true,
      },
    },
    className: {
      control: 'text',
      description: 'Кастомный CSS-класс',
      table: {
        disable: true,
      },
    },
    name: {
      control: 'text',
      description: 'Название группы радиобаттонов',
      defaultValue: '',
      type: { required: true },
    },
    value: {
      control: 'text',
      description: 'Значение',
      table: {
        summary: '123',
      },
      defaultValue: '',
    },
    onChange: {
      action: 'changed',
      table: {
        disable: true,
      },
    },
  }}
/>

# Radio

## Стилизованный радиобаттон

### Когда использовать

Когда есть необходимость добавить стилизованный радиобаттон на страницу.

### Примеры использования

Основной:

export const Template = (args) => <Radio {...args} />

<Canvas>
  <Story name="Default" story={stories.basic} />
</Canvas>

<ArgsTable story="Default" />

Пример с состоянием:

<Story name='WithState'>
  <WithState />
</Story>


Пример с "disabled" состоянием:

<Story name='DisableWithState'>
  <DisableWithState />
</Story>