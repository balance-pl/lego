// import { screen } from '@testing-library/react'

// HoC
import { render } from 'hoc/with-theme'

import Radio from './index'

describe('Radio', () => {
  it('Рендер Radio', () => {
    render(<Radio />)
  })
})
