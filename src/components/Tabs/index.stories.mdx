import { ArgsTable, Meta, Preview, Story } from '@storybook/addon-docs'
import Tabs from './index'
import {
  Default,
  Disabled,
  Secondary,
  SecondaryDisabled,
} from './index.stories'

<Meta
  title="Компоненты/Tabs"
  component={Tabs}
  argTypes={{
    appearance: {
      control: 'select',
      description: 'Внешний вид',
      defaultValue: 'primary',
      options: ['primary', 'secondary'],
    },
    disabled: {
      control: 'boolean',
      description: '"disabled" состояние',
      defaultValue: false,
    },
    className: {
      control: 'text',
      description: 'Кастомный CSS-класс',
      table: {
        disable: true,
      },
    },
    size: {
      control: 'select',
      description: 'размер',
      defaultValue: 'm',
      options: ['s', 'm', 'l'],
    },
    options: {
      control: 'object',
      description: 'список опций (табов)',
      defaultValue: [],
      table: {
        type: {
          summary: '[{id: string, name: string}]',
        },
      },
    },
    value: {
      control: false,
      description: 'Значение опции (таба) - id',
      table: {
        type: {
          summary: 'number, string, null',
        },
      },
      defaultValue: 'null',
    },
    onChange: {
      action: 'changed',
      description: 'Событие изменения значения',
      table: {
        type: {
          summary: 'func',
        },
      },
      defaultValue: '(value, e) => void',
    },
  }}
  parameters={{
    controls: {
      sort: 'alpha',
    },
  }}
/>

# Tabs

## Tabs - какое-то описание

Использовать, когда нужно предоставить выбор один из множества

### Примеры использования

export const Template = (args) => <Tabs {...args} />

Обычные tabs:

<Preview
  mdxSource={`
    <Tabs
      options={[
        { id: 1, name: 'Option 1' },
        { id: 2, name: 'Option 2' },
        { id: 3, name: 'Option 3' }
      ]}
      value={currentOption}
      onChange={handleChange}
    />
`}
>
  <Story
    name="Default"
    parameters={{
      controls: {
        hideNoControlsWarning: true,
        exclude: ['checked', 'boolean'],
      },
    }}
  >
    <Default />
  </Story>
</Preview>

"disabled" tabs:

<Preview
  mdxSource={`
    <Tabs
      disabled={true}
      options={[
        { id: 1, name: 'Option 1' },
        { id: 2, name: 'Option 2' },
        { id: 3, name: 'Option 3' }
      ]}
      value={currentOption}
      onChange={handleChange}
    />
`}
>
  <Story
    name="Disabled"
    parameters={{
      controls: false,
      include: ['onChange'],
      exclude: ['checked'],
    }}
  >
    <Disabled />
  </Story>
</Preview>

Внешний вид:

<Preview
  mdxSource={`
    <Tabs
      appearance={APPEARANCE.SECONDARY}
      options={[
        { id: 1, name: 'Option 1' },
        { id: 2, name: 'Option 2' },
        { id: 3, name: 'Option 3' }
      ]}
      value={currentOption}
      onChange={handleChange}
    />
`}
>
  <Story name="Secondary">
    <Secondary />
  </Story>
</Preview>

Внешний вид secondary, состояние disabled:

<Preview
  mdxSource={`
    <Tabs
      appearance={APPEARANCE.SECONDARY}
      disabled={true}
      options={[
        { id: 1, name: 'Option 1' },
        { id: 2, name: 'Option 2' },
        { id: 3, name: 'Option 3' }
      ]}
      value={currentOption}
      onChange={handleChange}
    />
`}
>
  <Story name="SecondaryDisabled">
    <SecondaryDisabled />
  </Story>
</Preview>

<ArgsTable story={'Default'} />

### CSS-классы

```scss
.Tab {
}
.Tab_active {
}
.Tabs {
}
.Tabs_disabled {
}
.Tabs_appearance_primary {
}
.Tabs_appearance_secondary {
}
.Tabs_disabled {
}
.Tabs_size_s {
}
.Tabs_size_m {
}
.Tabs_size_l {
}
```

''css
: string
''
