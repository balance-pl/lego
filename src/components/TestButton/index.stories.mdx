import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'

import TestButton from './index'

<Meta
  title="Компоненты/TestButton"
  argTypes={{
    appearance: {
      control: 'select',
      description: 'Внешний вид',
      options: ['primary', 'outline'],
    },
    children: {
      description: 'Содержимое кнопки',
    },
    className: {
      control: 'text',
      description: 'Кастомный CSS-класс',
    },
  }}
  component={TestButton}
  parameters={{
    actions: { argTypesRegex: '^on.*' },
  }}
/>

# TestButton

## Компонент-образец

Иногда хочется иметь перед глазами образец исходного кода компонента со всеми приблудами вроде stories.

export const Template = (args) => <TestButton {...args} />

<Canvas>
  <Story
    name="standard"
    args={{
      appearance: 'primary',
      children: 'TestButton',
      className: 'btn-custom-class',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="standard" />

### Когда использовать

Всякий раз, когда на странице нужен "кликабельный" элемент 👆

### Внешний вид

Внешний вид кнопки может принимать одно из следующих значений

<Story name="appearance">
  <div>
    <TestButton appearance="primary" onClick={(_) => _}>
      Primary
    </TestButton>{' '}
    <TestButton appearance="outline" onClick={(_) => _}>
      Outline
    </TestButton>
  </div>
</Story>
